# CLI Subcommand Contract: SubCheck (Orphan Unblock)

**Mode name**: `subcheck`
**Spec ref**: Spec 029 FR2 (orphan from Spec 028)
**Playbook file**: `subcontractor-agreement-v1.yaml` (existing on disk)
**BUNDLED_PLAYBOOKS key**: `subcheck` (already exists)
**MODE_VOCABULARY key**: `subcheck` (already exists)

## Interface

```bash
openreview subcheck <path> [options]
```

`<path>`: Path to a subcontractor agreement (PDF or DOCX).

## Options

Same flag set as ASSETCHECK.md. See [ASSETCHECK.md](./ASSETCHECK.md).

## Help text

```
Review a subcontractor agreement with SubCheck.
```

## Status

Orphan — CLI wiring only.
