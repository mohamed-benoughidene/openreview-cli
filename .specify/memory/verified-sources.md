# Verified Sources — Spec 022 Cleanup & Polish

Generated by web-research-grounding hook during `/speckit.plan`.

## Project Dependencies (already installed, no new deps required)

This spec adds zero new dependencies. All testing uses existing project infrastructure.

### pytest
- SOURCE: https://docs.pytest.org/en/stable/
- VERSION: 8.x (installed via dev dependency in pyproject.toml)
- KEY FACTS:
  - Python testing framework, fixture system, monkeypatch, tmp_path
  - `capsys` / `capfd` fixtures for stderr/stdout capture
  - `pytest.raises` for exception testing
  - `-m` marker filtering (unit, integration, memory, slow)
  - Already configured in `[tool.pytest.ini_options]` in pyproject.toml
- STATUS: CONFIRMED

### typer (CLI framework)
- SOURCE: https://typer.tiangolo.com/
- VERSION: 0.12.x (installed in pyproject.toml)
- KEY FACTS:
  - Built on Click, uses type hints for CLI argument/option definition
  - `CliRunner()` for CLI invocation tests
  - `result.exit_code` and `result.output` for assertion
  - Subcommand tree via `app.command()` and `app.add_typer()`
- STATUS: CONFIRMED

### presidio-analyzer / presidio-anonymizer (PII)
- SOURCE: https://microsoft.github.io/presidio/
- VERSION: 2.2.x (as recorded in pyproject.toml)
- KEY FACTS:
  - Analyzer: detects PII entities using NLP + regex + context
  - Anonymizer: replaces detected entities with placeholders
  - Supports custom recognizers
  - Built-in entities: PERSON, EMAIL, PHONE, CREDIT_CARD, etc.
  - spaCy `en_core_web_lg` as the NLP engine
- STATUS: CONFIRMED

### Existing Code Paths Referenced (no external source needed)

| Path | Purpose | Reference |
|------|---------|-----------|
| `src/openreview_cli/app.py` | CLI app, command tree | Spec R2, R3 — bilateral comparison subcommand |
| `src/openreview_cli/review/base.py` | Base review command class with PII orchestration | Spec R4, R5 — `--no-pii` flag |
| `src/openreview_cli/review/playbook.py` | Playbook loader | Spec R1 — `--playbook` / `--playbook-path` conflict |
| `src/openreview_cli/pii/engine.py` | PII detection/stripping engine | Spec R4 — skipped when `--no-pii` |
| `src/openreview_cli/gateway/router.py` | AI Gateway router | Spec R5 — text reaches gateway without PII when flag set |
| `src/openreview_cli/review/_gateway.py` | Shared gateway call helper | Mock target for integration tests |
| `tests/conftest.py` | Shared test fixtures | Existing `memory_tracker`, `fixtures_dir` |
| `tests/fixtures/` | Test data directory | Sample docs for bilateral comparison |

## Behavioral Claims (from spec)

1. `--no-pii` flag is already defined in review base command class (per T033-T035)
   - STATUS: CONFIRMED — spec states production code already shipped
2. Bilateral comparison subcommand already exists in app.py (per spec 014)
   - STATUS: CONFIRMED — spec states production code already shipped
3. Playbook precedence logic (name wins over path) already implemented — only warning message missing
   - STATUS: CONFIRMED — spec states production code already shipped
4. CI infrastructure (GitHub Actions, pytest, pre-commit) already configured
   - STATUS: CONFIRMED — visible in `.github/workflows/ci.yml`
5. Tests use mocking for AI gateway and PII engine
   - STATUS: CONFIRMED — test patterns from existing integration tests

## No NEEDS CLARIFICATION items

Spec 022 is unambiguous: no unknowns remain. All three scenarios are test-coverage-only additions to already-shipped production code.
