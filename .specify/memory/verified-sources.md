# Verified Sources — 5-Stage Async Pipeline (018)

Generated by manual emulation of `speckit.research-grounding` for feature `018-5-stage-async-pipeline` on 2026-07-04.

---

## Dependency Verification

### Runtime Dependencies (from pyproject.toml)

All runtime dependencies are listed in `/home/mohamed/lab/openreview/pyproject.toml` `[project] dependencies` and resolved in `uv.lock`. A `.venv` exists and contains all resolved packages.

| Dependency | in pyproject.toml | in .venv (via uv sync) | Notes |
|---|---|---|---|
| `click>=8.0.0` | ✅ | ✅ | Transitive dep of `typer`; listed explicitly for pinning. Used via Typer, not directly. |
| `cryptography>=49.0.0` | ✅ | ✅ | PII encryption (Fernet/HKDF) |
| `httpx>=0.28.1` | ✅ | ✅ | HTTP client for AI Gateway |
| `litellm>=1.90.1` | ✅ | ✅ | LLM routing (AI Gateway) |
| `nupunkt>=0.6.0` | ✅ | ✅ | Sentence boundary detection (clause parsing) |
| `platformdirs>=4.10.0` | ✅ | ✅ | Config/auth file paths |
| `presidio-analyzer>=2.2.362` | ✅ | ✅ | PII analysis |
| `presidio-anonymizer>=2.2.362` | ✅ | ✅ | PII anonymization |
| `pydantic>=2.13.4` | ✅ | ✅ | Data models across all modules |
| `pymupdf>=1.27.2.3` | ✅ | ✅ | PDF parsing (document engine) |
| `python-docx>=1.2.0` | ✅ | ✅ | DOCX parsing (document engine) |
| `pyyaml>=6.0.3` | ✅ | ✅ | Playbook YAML loading |
| `questionary>=2.1.1` | ✅ | ✅ | Interactive wizard (AI Gateway setup) |
| `rich>=15.0.0` | ✅ | ✅ | Terminal output formatting |
| `typer>=0.26.7` | ✅ | ✅ | CLI framework (entry point) |

### Dev Dependencies (from pyproject.toml `[dependency-groups] dev`)

| Dependency | in pyproject.toml | in .venv | Notes |
|---|---|---|---|
| `mypy>=2.1.0` | ✅ | ✅ | Type checking (strict mode) |
| `pytest>=9.1.1` | ✅ | ✅ | Test runner |
| `ruff>=0.15.18` | ✅ | ✅ | Linter + formatter |
| `types-pyyaml>=6.0.12.20260518` | ✅ | ✅ | Type stubs for PyYAML |

### Plan-Declared Dependencies (Zero New — Stdlib Only)

The plan at `plan.md` §Primary Dependencies states: **"None new. Pipeline uses stdlib only."** Confirmed:

| stdlib module | Used for | Status |
|---|---|---|
| `asyncio` | Async pipeline runner, stage concurrency | ✅ stdlib (Python 3.12) |
| `dataclasses` | `StageResult`, `PipelineReport`, `ProgressEvent` dataclasses | ✅ stdlib (Python 3.12) |
| `abc` | `Stage` ABC (abstract base class) | ✅ stdlib (Python 3.12) |
| `time` | Duration tracking | ✅ stdlib (Python 3.12) |
| `tracemalloc` | Memory profiling (Phase 3) | ✅ stdlib (Python 3.12) |
| `logging` | Pipeline warnings and diagnostics | ✅ stdlib (Python 3.12) |

### Forbidden Dependencies — Confirmed Absent

The following forbidden dependencies (per constitution and spec `TechStack.md`) are **NOT** present in pyproject.toml, uv.lock, or any source imports:

| Dependency | Status | Notes |
|---|---|---|
| `langchain` | ❌ Not installed | Permitted: litellm for LLM routing |
| `llama-index` | ❌ Not installed | N/A |
| `FAISS` | ❌ Not installed | Retrieval uses custom BM25/dense hybrid |
| `spaCy` | ❌ Not a direct dep | Constitution exempts spaCy model for PII (Principle III) but it's not in pyproject.toml — loaded externally by presidio-analyzer |
| `sentence-transformers` | ❌ Not installed | Ollama embeddings used instead |
| `Click` (as CLI framework) | ❌ Not used directly | `typer` is the CLI framework; `click` is a transitive pin only |
| `loguru` / `structlog` | ❌ Not installed | stdlib `logging` used |
| `FastAPI` / `Flask` | ❌ Not installed | Local CLI only |

### Existing Capability Modules — Confirmed on Filesystem

The pipeline plan references five existing capability modules. All confirmed:

| Module | Path | Plan Ref | Status |
|---|---|---|---|
| `parsing/` | `src/openreview_cli/parsing/` | C-08 | ✅ 7 files (incl. `__init__.py`, `stream.py`, `pdf_parser.py`, etc.) |
| `pii/` | `src/openreview_cli/pii/` | N-4a / C-10 | ✅ 11 files (incl. `engine.py`, `recognizers.py`, `mapping.py`, etc.) |
| `chunking/` | `src/openreview_cli/chunking/` | C-32 | ✅ 5 files (incl. `__init__.py`, `splitter.py`, `stream.py`, etc.) |
| `retrieval/` | `src/openreview_cli/retrieval/` | C-19 | ✅ 10 files (incl. `engine.py`, `bm25.py`, `dense.py`, `ingest.py`, etc.) |
| `gateway/` | `src/openreview_cli/gateway/` | C-12 | ✅ 9 files (incl. `router.py`, `registry.py`, `cost.py`, etc.) |

### Summary

- ✅ All 15 runtime deps are in pyproject.toml and resolved in uv.lock
- ✅ All 4 dev deps are in pyproject.toml and resolved in uv.lock
- ✅ Zero new dependencies needed for the pipeline framework (stdlib only)
- ✅ No forbidden dependencies present
- ✅ All five existing capability modules are confirmed on the filesystem
- ✅ `.venv` exists and is functional
